export const metadata = {
  title: 'Dashboard | Susan Makeup',
  description: 'Admin Dashboard',
};

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
